<!DOCTYPE html>
<html>
<head>
    <%- include('../parts/head'); %>
</head>
<body>
    <%- include('../parts/header'); %>

<div style="width: 100%;">
    <a href="views/showAddCategory" class="btn btn-primary" class="mb-2">Add new</a>
    <table class="table table-hover">
        <thead>
            <tr>
            <th scope="col">№</th>
            <th scope="col">Title</th>
            <th scope="col">Manage</th>
            </tr>
        </thead>
        <tbody>
            <% categories.forEach(element => { %>
                <tr>
                    <th scope="row"><%= element.id %></th>
                    <td><%= element.title %></td>
                    <td><button type="button" class="btn btn-warning mx-2">Edit</button><a href="/views/showDeleteCategory/<%= element.id %>" class="btn btn-danger">Delete</a></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</div>
<%- include('../parts/footer'); %>
</body>
</html>